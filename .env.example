# Application Environment
NODE_ENV="development" # Use "production" to enable CORS

# CORS Configuration (only used in production)
CORS_ORIGINS="http://localhost:5500,http://127.0.0.1:5500" # Comma-separated list of allowed origins
